export const projectTabs = ['Dati progetto', 'Variabili', 'Immagini', 'Note'] as const
export const pinnedProjectIdsStorageKey = 'app-control-pinned-project-ids'

export const defaultSyncPrompt =
  'Sincronizza questo progetto con App Control. Leggi il file `.agent/app-control.json` nella root del progetto. Se esiste, usa `projectId`, `agentKey`, `appControlSupabaseUrl` e `appControlSupabaseAnonKey` per leggere da App Control via Supabase REST inviando gli header `x-app-control-project-id` e `x-app-control-agent-key`. Se il file `.agent/app-control.json` non esiste, fermati: AZIONE ORA: apri App Control, vai sul progetto, apri il tab Dati progetto e in fondo copia il contenuto di `.agent/app-control.json`, poi incollalo qui. (1) Aggiungi `.env`, `.mcp.json` e `.agent/` al `.gitignore`. (2) Scarica il prompt `CLAUDE.MD` con la query `GET /rest/v1/prompts?title=eq.CLAUDE.MD&select=full_text` (titolo esatto, maiuscolo) e scrivi il campo `full_text` come file `CLAUDE.md` nella root del progetto, poi applicane le regole al 100%. Idempotente: se `CLAUDE.md` esiste gia ed e identico, NON riscaricarlo; se la query torna vuota, fermati e segnalalo (non proseguire senza regole). Da li Claude Code lo carica da solo a ogni sessione: e la cache locale, non rileggerlo dal DB a ogni messaggio. (3) Recupera i dati canonici da `project_env_variables`, `project_data_fields` e `projects`. VALORE VARIABILI: ogni riga di `project_env_variables` ha `value_text` (per le NON sensibili) e `value_ciphertext` (per le sensibili, `is_sensitive=true`); per ciascuna leggi il campo giusto in base a `is_sensitive`, MAI la colonna `value`. Leggere solo `value_text` lascerebbe VUOTE tutte le sensibili (token, chiavi, DATABASE_URL): nel `.env` non devono mai finire valori vuoti. (4) Genera o aggiorna `.env` con tutte le variabili del progetto; se il progetto usa Vite, deriva `VITE_SUPABASE_URL` da `SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY` da `SUPABASE_ANON_KEY`; se serve `SUPABASE_DB_URL` trattala come alias di `DATABASE_URL`. (5) Genera `.mcp.json` con i server MCP GitHub (usando GITHUB_TOKEN) e Supabase (usando SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY). (6) Configura il remote git origin col token: `git remote set-url origin https://GITHUB_TOKEN@github.com/REPO_PATH.git` (GITHUB_TOKEN reale, REPO_PATH estratto da GITHUB_URL), cosi push e pull usano sempre le credenziali del progetto, indipendentemente dall\'account gh globale. (7) RICONCILIA SEMPRE le variabili `.env` <-> App Control: a ogni sync confronta le chiavi del `.env` reale con quelle gia presenti in `project_env_variables` e CARICA in App Control ogni variabile/segreto mancante o cambiato (es. `SESSION_SECRET`, chiavi di altre piattaforme, URL di servizio). ESCLUDI dall\'upload solo: le 9 variabili manuali dell\'utente (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, `RENDER_API_KEY`, `GITHUB_URL`, `GITHUB_TOKEN`, `LINK_DEPLOY`, `LINK_DEPLOY ADMIN`) e le derivate `VITE_*`/`SUPABASE_DB_URL` (si rigenerano dal `.env`, non si archiviano). Per ogni INSERT in `project_env_variables`: includi `project_id` (UUID del progetto, ottenuto dalla SELECT su `projects`, NON lo slug — altrimenti la RLS nega con 401), nome canonico, e metti il valore in `value_ciphertext` con `is_sensitive=true` se e un segreto (token/chiave/URL con credenziali), altrimenti in `value_text` con `is_sensitive=false`. Se non ci sono variabili nuove, non scrivere nulla. Queste appariranno nella sezione "Gestite da Agent". (8) DUE LINK DI DEPLOY (MANUALI): `LINK_DEPLOY` (URL pubblico/user) e `LINK_DEPLOY ADMIN` (URL reale dell\'area admin/gestione di QUESTO progetto) li inserisce l\'UTENTE a mano nel box "Da inserire manualmente" della sezione Variabili e si mostrano come link sotto al titolo del progetto. L\'Agent li LEGGE (per popolare il `.env` se servono) ma NON li scrive ne li sovrascrive in App Control: sono di competenza dell\'utente. Se sono vuoti e conosci i link reali dal deploy, segnalali all\'utente perche li inserisca a mano, senza scriverli tu. (9) Verifica le connessioni a GitHub, Supabase e Render in sola lettura, senza dichiararle ok senza prova. Non stampare mai token, password, chiavi o segreti in chat o log; prima di qualsiasi commit o push chiedi conferma esplicita. Al termine, se previsto, avvia l\'app in locale sulla porta 5001.'

export type ProjectTab = (typeof projectTabs)[number]
export type ProjectListSortMode = 'recent-desc' | 'name-asc' | 'name-desc'
export type ProjectSaveState = 'idle' | 'saving' | 'saved' | 'error'
